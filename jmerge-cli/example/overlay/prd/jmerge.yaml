namespace: prd

# path of base file
base: ../../base
output: ../../output

# true is format output json
format: true

# merge target
merges:
  # mode is add or ignore
  - mode: ignore
    targets:
      - http/setting.json